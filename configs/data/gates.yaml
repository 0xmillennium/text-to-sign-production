# Structural usability decisions
# Defines the absolute gating rules that reject corrupted or structurally unusable data

min_valid_frames: 1
max_out_of_bounds_ratio: 0.5
min_any_hand_nonzero_frames: 1

channels:
  body:
    min_nonzero_frames: 1
  face:
    min_nonzero_frames: 1
